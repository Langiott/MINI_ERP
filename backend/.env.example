# Copia questo file in .env e adatta i valori.
# La porta e' 5433 perche' e' quella esposta dal container in docker-compose.yml.

DATABASE_URL="postgresql://postgres:postgres@localhost:5433/mini_erp?schema=public"
PORT=3000
